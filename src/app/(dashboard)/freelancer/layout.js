export default function FreelancerLayout({ children }) {
  return (
    <section>
      {/* Aquí podrías poner una barra lateral o navegación específica para freelancers en el futuro */}
      {children}
    </section>
  );
}